const mysql = require('mysql2');const connection = mysql.createConnection({    host: '127.0.0.1',    user: 'root',    password: '', });connection.connect(async (err) => {    if (err) return console.error('Erro:', err);    console.log('??? Apagando banco de dados antigo...');        // Apaga o banco inteiro para recriar do zero    connection.query("DROP DATABASE IF EXISTS sistema_gestao_notas", (err) => {        if(err) console.error(err);        else console.log('? Banco limpo! Agora rode "node server.js" para instalar o novo.');        process.exit();    });});